import Button from '../button/primary'

const Contact = () => {
  return (
    <section className="contact wrapper">
      <div>
        <p className="test">Get in touch using the details below</p>
      </div>
    </section>
  )
}

export default Contact
